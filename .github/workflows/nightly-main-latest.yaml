name: Nightly CLI test for main

on:
  schedule:
    - cron: '08 1,13 * * *'
  # TEMPORARY: Enable for PR testing - REVERT AFTER TESTING
  push:
    branches:
      - create_hub_instance_for_ccm_tests
  pull_request:
    branches:
      - main

jobs:
  test-suite:
    # TEMPORARY: Use fork repository for PR testing - REVERT to konveyor/kantra-cli-tests/.github/workflows/test-suite.yaml@main AFTER TESTING
    uses: sshveta/kantra-cli-tests/.github/workflows/test-suite.yaml@create_hub_instance_for_ccm_tests
    secrets: inherit
    with:
      tag: latest
      tier: TIER0
