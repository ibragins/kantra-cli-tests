name: Nightly CLI test for main

on:
  schedule:
    - cron: '0 1 * * *'

jobs:
  test-suite-containerless:
    # TODO: Add other provider tests
    uses: ./.github/workflows/test-suite-java-containerless.yaml
    secrets: inherit
    with:
      tag: latest
